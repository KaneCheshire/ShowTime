use_frameworks!

target 'ShowTime_Example' do
  pod 'ShowTime', :path => '../'

  target 'ShowTime_Tests' do
    inherit! :complete
    pod 'PixelTest', '0.7.0'
  end
end

target 'ShowTimeTodayExtension' do
    pod 'ShowTime', :path => '../'
end
